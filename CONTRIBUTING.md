# Contributing
Use synthetic data only. Add tests and documentation, then run `ruff check .` and `pytest -q`. Contributions must not add diagnosis, prescribing, treatment recommendations, patient-specific advice, real PHI, or unsupported clinical claims.
